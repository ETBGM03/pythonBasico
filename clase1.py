# operadores aritmeticos
# import math
# print(math.sqrt(12))

# LAS VARIBALES
# Son espacios en memoria que almacenan informacion
# reglas:
# * no comenzar con numero
# * no usar matusculas
# * si hay mas de una palabra separarlas con _

# DATOS PIRMITIVOS

# * string = cadena de caracteres
# * boolean = True o False
# * entero = 2,3,4
# * decimales = 2.3, 4.6 (importante que en python los decimales se definen con .)

# CONVERTIR DATOS
# de string a int: variable =  int(variable_a_convertir)
# de int a string: str(varibale_a_convertir)

# OPERADORES LOGICOS Y DE COMPARACION
# LÓGICOS
# 1 and: este operador lógico me devolverá True sólo si las comparaciones son True, de lo constrario devolver False
# 2 or: este operador lógico me devolverá False cuando ambas comparaciones son False, de lo contrario devolverá True
# 3 not: este cambiará el estado de una variable boolean; de True a False o de False a True

# COMPARACION
# 1 == (igual?): comparacion de variables
# 2 != (distinto): verificar si son distintos
# > (mayor?)
# < (menor?)
# >= (mayor o igual?)
# <= (menor o igual?)


# TRABAJANDO CON TEXTO

# el metodo .upper() convierte un texto minuscuka en mayuscula
# el mentodo .capitalize() convierte la primera palabra en mayuscula; pero para cambiar el valor en la variable debo hacer la conversion
# el metodo .stipt() eliminar todos los espacios
# el metodo .lower() convierte todo en minuscula
# el emetodo .replace(para1, para2)= para1(letra_reemplazada) para2( letra_reemplazar)
# indices = para acceder a una letra en especifico se usa []
# el metodo len() para saber el numero de letras de un cadena:

# -SLICE
# esto nos permite "cortar rebanadas" las cadenas de caracteres
# se hace con variable[inicial:final]; variable[inicial:final:pasos_a_saltar]
# variable[::] => variable completo
# variable[inicial::paso_a_salatar]
# variable[::-1] => de atrás palante
